interface Kitchen{
    _id ?: string,
    itemName ?: string,
    category ?: string,
    price ?: string,
    description ?: string,
    isListed ?: boolean,
    image :string
}

export default Kitchen